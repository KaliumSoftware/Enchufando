const orderRouter = require('./orderRouter');
const productRouter = require('./productRouter');
const userRouter = require('./userRouter');

module.exports = { orderRouter, productRouter, userRouter };
